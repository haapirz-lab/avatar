fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
edge-tts>=7.0,<8.0
openai>=1.40,<2.0          # also used as the Groq client (OpenAI-compatible API)
python-multipart>=0.0.9
pydantic>=2.7,<3.0
# Optional: offline sentiment fallback when no API key is set.
transformers>=4.40
torch>=2.2
